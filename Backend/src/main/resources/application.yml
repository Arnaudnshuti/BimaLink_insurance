spring:
  application:
    name: bimalink
  datasource:
    url: jdbc:postgresql://localhost:5432/bimalink_db
    username: bima_user
    password: StrongPassword123
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: false
  flyway:
    enabled: true
    baseline-on-migrate: true
    locations: classpath:db/migration
  mail:
    host: smtp.gmail.com
    port: 587
    username: arnaudnshutidjadja@gmail.com
    password: drqk jvwh xxmn wacj
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
          ssl:
            enable: false
          connectiontimeout: 5000
          timeout: 5000
          writetimeout: 5000

server:
  port: 8081

# Security configuration
app:
  jwt:
    secret: ${JWT_SECRET:change-this-secret-key-to-something-secure-in-production}
    expiration: 86400000 # 24 hours in milliseconds
  cors:
    allowed-origins: http://localhost:4200
  otp:
    expiry-minutes: 10
  email:
    from: arnaudnshutidjadja@gmail.com

# TODO: Add payment provider credentials
payment:
  provider:
    api-url: https://sandbox-payment-provider.example.com/api
    api-key: ${PAYMENT_PROVIDER_API_KEY:your-api-key-here}
    callback-secret: ${PAYMENT_PROVIDER_CALLBACK_SECRET:your-callback-secret-here}
